load("//build_utils:defs.bzl", "strict_cc_library")

strict_cc_library(
    name = "rest_api_helper",
    srcs = ["rest_api.cpp"],
    hdrs = ["rest_api.hpp"],
    visibility = ["//visibility:public"],

    deps = [
        "@cpp-httplib",
        "@nlohmann_json//:json",
    ],
)
