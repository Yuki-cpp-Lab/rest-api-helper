load("@build_utils//:defs.bzl", "strict_cc_test")

strict_cc_test(
    name = "unit_tests",
    srcs = ["rest_api_test.cpp"],
    deps = [
        "//src:rest_api_helper",
        "@googletest//:gtest",
        "@googletest//:gtest_main",
    ],
)
